apiVersion: v1
kind: Service
metadata:
  name: simple-app-svc
  labels:
    app: simple-api
spec:
  type: LoadBalancer
  selector:
    app: simple-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
