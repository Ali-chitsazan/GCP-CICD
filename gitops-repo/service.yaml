apiVersion: v1
kind: Service
metadata:
  name: simple-app-svc
  labels:
    app: simple-api
spec:
  type: LoadBalancer
  selector:
    app: simple-api
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
