apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: simple-api
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: simple-api
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
